---
// Hero component - Full-screen hero section with background image
---

<section class="hero relative h-screen w-full overflow-hidden">
  <!-- Background Image -->
  <div class="absolute inset-0 z-0">
    <img
      src="https://picsum.photos/1920/1080?grayscale"
      alt="Hero background"
      class="h-full w-full object-cover opacity-60"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-charcoal/50 to-charcoal/30"></div>
  </div>

  <!-- Hero Content -->
  <div class="hero-content relative z-10 flex h-full flex-col items-center justify-center px-6 text-center text-cream">
    <h1 class="fade-up mb-6 font-serif text-6xl font-bold leading-tight md:text-7xl lg:text-8xl">
      Collective<br />Architecture
    </h1>
    <p class="fade-up max-w-2xl text-lg font-light tracking-wide md:text-xl">
      Crafting spaces that blend innovation with timeless elegance
    </p>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 z-10 -translate-x-1/2 animate-bounce">
    <svg
      class="h-8 w-8 text-cream"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M19 14l-7 7m0 0l-7-7m7 7V3"
      ></path>
    </svg>
  </div>
</section>

<style>
  .hero-content > * {
    opacity: 0;
    transform: translateY(30px);
  }

  .hero-content > *:nth-child(1) {
    animation: fadeUp 1s ease-out forwards;
    animation-delay: 0.2s;
  }

  .hero-content > *:nth-child(2) {
    animation: fadeUp 1s ease-out forwards;
    animation-delay: 0.5s;
  }

  @keyframes fadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
